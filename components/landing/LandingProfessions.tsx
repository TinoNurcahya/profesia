import Link from "next/link";
import { getTranslations } from "next-intl/server";
import { ArrowRight, DollarSign, TrendingUp } from "lucide-react";

type Profession = { id: number; slug: string; category_name_id: string; category_name_en: string; name_id: string; name_en: string; description_id: string; description_en: string; prospects: string; salary_min: number; salary_max: number; matched_mbti: { code: string }[] };

export default async function LandingProfessions({ locale, professions }: { locale: string; professions: Profession[] }) {
  const t = await getTranslations("Landing.exploration");
  return <section className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 space-y-8"><div className="flex flex-col sm:flex-row items-start sm:items-end justify-between gap-4"><div className="space-y-2"><h2 className="text-3xl font-bold tracking-tight text-slate-900 dark:text-white">{t("title")}</h2><p className="text-slate-600 dark:text-slate-400 text-sm max-w-xl">{t("subtitle")}</p></div><Link href={`/${locale}/professions`} className="inline-flex items-center gap-2 text-sm font-bold text-indigo-600 dark:text-indigo-400 hover:text-indigo-700 dark:hover:text-indigo-300 group">{t("viewAll")}<ArrowRight className="w-4 h-4 group-hover:translate-x-1 transition-transform" /></Link></div><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">{professions.slice(0, 6).map((prof) => <div key={prof.id} className="glass-card rounded-3xl p-6 flex flex-col justify-between space-y-5 group"><div className="space-y-3"><div className="flex items-center justify-between"><span className="px-3 py-1 rounded-full text-xs font-semibold bg-indigo-50 dark:bg-indigo-950 text-indigo-600 dark:text-indigo-300 border border-indigo-100 dark:border-indigo-800">{locale === "id" ? prof.category_name_id : prof.category_name_en}</span><span className="inline-flex items-center gap-1 text-xs font-bold text-emerald-600 dark:text-emerald-400 bg-emerald-50 dark:bg-emerald-950/60 px-2.5 py-1 rounded-full"><TrendingUp className="w-3.5 h-3.5" />{t("prospectLabel", { value: prof.prospects === "high" ? t("highProspect") : prof.prospects })}</span></div><h3 className="text-xl font-bold text-slate-900 dark:text-white group-hover:text-indigo-600 dark:group-hover:text-indigo-400 transition-colors">{locale === "id" ? prof.name_id : prof.name_en}</h3><p className="text-xs text-slate-600 dark:text-slate-300 line-clamp-2 leading-relaxed">{locale === "id" ? prof.description_id : prof.description_en}</p><div className="p-3 rounded-2xl bg-slate-100/80 dark:bg-slate-800/80 flex items-center justify-between text-xs"><span className="text-slate-500 dark:text-slate-400 font-medium">{t("salaryLabel")}</span><span className="font-extrabold text-slate-900 dark:text-white flex items-center gap-1"><DollarSign className="w-3.5 h-3.5 text-emerald-500" />{t("salaryValue", { min: (prof.salary_min / 1000000).toFixed(0), max: (prof.salary_max / 1000000).toFixed(0) })}</span></div></div><div className="pt-3 border-t border-slate-100 dark:border-slate-800 flex items-center justify-between"><div className="flex items-center gap-1 text-xs text-slate-500"><span className="font-semibold text-slate-700 dark:text-slate-300">{t("mbtiMatchLabel")}</span><div className="flex gap-1">{prof.matched_mbti.slice(0, 2).map((m) => <span key={m.code} className="px-1.5 py-0.5 rounded bg-purple-100 dark:bg-purple-950 text-purple-700 dark:text-purple-300 font-bold text-[10px]">{m.code}</span>)}</div></div><Link href={`/${locale}/professions/${prof.slug}`} className="px-4 py-2 rounded-xl text-xs font-bold bg-slate-900 dark:bg-slate-100 text-white dark:text-slate-900 hover:bg-indigo-600 dark:hover:bg-indigo-400 transition-colors">{t("detailCta")}</Link></div></div>)}</div></section>;
}
